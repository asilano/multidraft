%tr
  %td.auth-nickname= authentication.nickname
  %td
    = link_to remove_authentication_path(authentication), :method => :delete, :remote => true, :class => 'auth-remove', :data => {:replace => '#omniauth_fields'} do
      %span.icon-cancel-circle{'aria-hidden'=>true}>
      Remove
